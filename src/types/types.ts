export interface IDate {
    dateStr: {
        day: string,
        year: string,
        month: string
    },
    timeStr: {
        hours: string,
        minutes: string,
        seconds: string
    }
}